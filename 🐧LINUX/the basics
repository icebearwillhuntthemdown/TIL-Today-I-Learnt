### Commands, terminal, and shell
Commands are the text you type in the terminal, and are interpreted by the shell.   
Bash is a type of shell, which is the most common one and the default shell of Linux.

### Command Structure
    commandName + options + inputs

